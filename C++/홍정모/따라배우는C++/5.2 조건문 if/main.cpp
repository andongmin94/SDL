#include <iostream>

using namespace std;

int min(int x, int y)
{
	if (x > y) 
		return y;
	else
		return x;

	/*return (x > y) ? y : x;*/
}

int main()
{
	int x;
	cin >> x;

	if (x = 0)
		cout << x << endl;

	cout << x << endl;
}