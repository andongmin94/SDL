#include <iostream>

using namespace std;

int choice(int* arr, int N);

int main()
{
    int N, M;
    cin >> N >> M;
    int* arr = new int[N];
    int* visited = new int[N];
    int cnt[8][8] = { 0, };

    for (int i = 0; i < N; i++)
    {
        arr[i] = i + 1;
        visited[i] = 0;
    }
}

int choice(int* arr, int N)
{
    if (N - 1 != 0)
        

    
}