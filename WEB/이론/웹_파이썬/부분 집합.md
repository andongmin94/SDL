# 현강
```python
# subset_test
# 부분집합 만들기
# 해당 원소를 사용하면 1, 해당 원소를 사용하지 않으면 0
# N : 배열의 길이, i : 재귀호출한 깊이
def subset(N, i):
    # 기저조건
    if i == N:
        total = 0
        # bits 1 유, 0 무
        for j in range(N):
            if bits[i] == 1: # 해당 원소를 사용
                # 요소 값을 더해서
                total += arr[i]
        if total == 0:
            for j in range(N):
                if bits[i] == 1:
                    print(arr[i])
        return

    # i 번째에 있는 요소를 사용
    bits[i] = 1
    subset(N, i + 1)
    
    # i 번째에 있는 요소를 사용 x
    bits[i] = 0
    subset(N, i + 1)    


arr = [-1,3,-9,6,7,-6,1,5,4,-2]
N = len(arr)
bits = [0] * N
subset(N, 0)
```

